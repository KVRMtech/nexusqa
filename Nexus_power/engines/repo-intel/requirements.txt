# repo-intel engine (S3) — repository intelligence.
# SDK (NexusEngine, EngineConfig, envelope encryption) — editable, matches
# every other engine; stripped by the Dockerfile (the base image preinstalls
# the SDK).
-e ../../sdk/nexus-sdk

# Manifest / OpenAPI spec parsing (openapi_spec extractor, later stage).
pyyaml>=6.0,<7.0

# tree-sitter native parsing for the ts-routes / express-nest / spring
# extractors (later stages). Pinned but IMPORT-GUARDED: the grammar wheels
# may not install in every environment, so those extractors gate the native
# path behind an availability check and fall back to a lightweight text
# scan (design §3.3 + Windows hard constraint). repo-intel Stage-1 imports
# NEITHER of these — they carry no import-time dependency here.
tree-sitter>=0.21,<0.22
tree-sitter-languages>=1.10,<2.0

# NOTE: git is a SYSTEM binary (installed by the Dockerfile), not a pip
# dependency — the connector shells out to it via subprocess so the token
# is passed through the environment, never through command-line args.
