"""QE-Central Contained Explorer (S2) — crawler-as-recorder service package.

Pure-logic modules (inventory, fingerprint, guard) are importable without a
browser or DB so they can be unit-tested in isolation.
"""
